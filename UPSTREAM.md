# officer.dev upstream status

`officer.dev` is a development fork of [`officer`](https://github.com/davidgohel/officer).

The R package name remains `officer` for full compatibility with the
existing ecosystem (mschart, flextable, rvg, officedown).

## Integrated upstream PRs

| PR | Description | Upstream status | officer.dev |
|---|---|---|---|

## Additional officer.dev changes

| Description | Branch/Commit |
|---|---|
| `phs_annotate()`, `add_annotated_layouts()`, `annotate_base()` rewrite (#682) | `f24b2e14` |
| Use `...` modifiers in `ph_with()` methods (#681) | `378c675b` |
